@model IEnumerable<AzmanAzWebPage.Models.Kategoriya>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Az/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="text-center">Məhsul növü</h2>

<p>
    @Html.ActionLink("Əlavə et", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Növ
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.kategorya_adi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.kategorya_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.kategorya_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.kategorya_id })
        </td>
    </tr>
}

</table>
